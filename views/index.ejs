<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Verdant API</title>
  <style>
    :root {
      --primary-color: #228B22;
      --primary-hover: #1e7e1e;
      --background-color: #f0f4f1;
      --text-color: #2f2f2f;
      --card-bg: #ffffff;
      --shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body, html {
      height: 100%;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: var(--background-color);
      color: var(--text-color);
    }

    .container {
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .card {
      background-color: var(--card-bg);
      padding: 3rem 4rem;
      border-radius: 16px;
      box-shadow: var(--shadow);
      text-align: center;
      max-width: 600px;
      width: 100%;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: var(--primary-color);
    }

    p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      color: #555;
    }

    a.button {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      font-size: 1rem;
      font-weight: bold;
      color: white;
      background-color: var(--primary-color);
      text-decoration: none;
      border-radius: 8px;
      transition: background-color 0.3s;
    }

    a.button:hover {
      background-color: var(--primary-hover);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card">
      <h1>Verdant API</h1>
      <p>Welcome to the Verdant API. Access REST endpoints or explore the interactive documentation.</p>
      <a href="/api" class="button">View Swagger Documentation</a>
    </div>
  </div>
</body>
</html>
