<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />

		<title>Verdant API</title>
		<link rel="icon" href="/favicon.svg" type="image/png" />
		<style>
			:root {
				--primary-color: #2f855a;
				--primary-hover: #165a16;
				--background-color: #f0f4f1;
				--text-color: #2f2f2f;
				--card-bg: #ffffff;
				--shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
			}

			* {
				box-sizing: border-box;
				margin: 0;
				padding: 0;
			}

			body,
			html {
				height: 100%;
				font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
				background-color: var(--background-color);
				color: var(--text-color);
			}

			.container {
				height: 100%;
				display: flex;
				align-items: center;
				justify-content: center;
				padding: 2rem;
			}

			.card {
				background-color: var(--card-bg);
				padding: 3rem 4rem;
				border-radius: 16px;
				box-shadow: var(--shadow);
				text-align: center;
				max-width: 600px;
				width: 100%;
			}

			.logo {
				max-width: 200px;
				margin-bottom: 1rem;
			}

			p {
				font-size: 1.2rem;
				margin-bottom: 0.5rem;
				color: #555;
			}

			.info {
				font-size: 1rem;
				color: #a09e9e;
				margin-bottom: 2rem;
			}

			a.button {
				display: inline-block;
				padding: 0.75rem 1.5rem;
				font-size: 1rem;
				font-weight: bold;
				color: white;
				background-color: var(--primary-color);
				text-decoration: none;
				border-radius: 8px;
				transition: background-color 0.3s;
			}

			a.button:hover {
				background-color: var(--primary-hover);
			}
		</style>
	</head>
	<body>
		<div class="container">
			<div class="card">
				<img src="/logo.png" alt="Verdant Logo" class="logo" />
				<p>
					Welcome to the Verdant API. Access REST endpoints or explore the
					interactive documentation.
				</p>
				<p class="info">Please take care, and do not destroy my little app 😊</p>
				<a href="/api" class="button">View Swagger Documentation</a>
			</div>
		</div>
	</body>
</html>
